# elm-semantic-ui

Elm bindings for Semantic UI.

```Elm
import Html exposing (Html)
import SemanticUi exposing (..)

view : Html
view =
    button "click me!"
        |> size Large
        |> render
```
